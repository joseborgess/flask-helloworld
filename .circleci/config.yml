# Python CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-python/ for more details

version: 2

jobs:

    build:
        working_directory: ~/flask-helloworld

        docker:
            - image: circleci/python:3.8.6-buster

        steps:
            - checkout
            - run:
                name: Install Dependencias
                command: |
                     pip install --no-cache-dir -r requirements.txt
                    

            - run:
                name: Install Scanner
                command: |
                     wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.7.0.2747-linux.zip
                     unzip sonar-scanner-cli-4.7.0.2747-linux.zip
            
            - run:
                name: Run Sonarscanner
                command: |
                    
                    export SONAR_SCANNER_OPTS="-Xmx2048m"
                    eval ./sonar-scanner-4.7.0.2747-linux/bin/sonar-scanner -Dsonar.host.url=http://ec2-44-197-118-108.compute-1.amazonaws.com:9000/ \
                    -Dsonar.sources=. \
                    -Dsonar.login=6e15012a942a49d60652e1cc8d9a8ec1abb28ca2

                
           